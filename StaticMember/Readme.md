# What is static member ?

> In C++ Object-Oriented Programming (OOP), a static member is a class member that belongs to the class itself rather than to any specific instance (object) of that class.

## Key Characteristics

- They are attribute of classes or class member
- It is declared using static keyword
- only one copy of that member is created for the entire class & is shared by all the object
- It is initialized only once

